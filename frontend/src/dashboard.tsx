import { Container, Typography } from '@mui/material';

export default function Dashboard(): JSX.Element {
    return <Container style={{paddingBottom: '5em'}}>
        <Typography variant='h6'>What we have found</Typography>
        </Container>
}